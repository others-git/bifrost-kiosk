<?xml version="1.0" encoding="utf-8"?>
<!-- The DecoratedBarcodeView is added programmatically in ScanActivity (index 0,
     behind these overlays) — inflating it from XML crashed on this build. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scanRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Big, unmissable result banner: turns green the instant a code decodes. -->
    <TextView
        android:id="@+id/scanBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:padding="16dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:background="#CC101418"
        android:text="@string/scan_prompt" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center"
        android:background="#CC101418">

        <Button
            android:id="@+id/torchButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            android:text="@string/scan_torch_on" />

        <Button
            android:id="@+id/cancelButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/scan_cancel" />
    </LinearLayout>
</FrameLayout>
